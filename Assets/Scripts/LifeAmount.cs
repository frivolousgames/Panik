using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using UnityEngine.UI;

public class LifeAmount : MonoBehaviour
{
    /*int lifeAmount;
    Slider lifeMeter;

    void Awake()
    {
        lifeAmount = (int)GameController.lifeAmount;
        lifeMeter = GetComponent<Slider>();
        //lifeMeter.value = lifeAmount;
    }

    void Update()
    {
        lifeAmount = (int)GameController.lifeAmount;
        lifeMeter.value = lifeAmount;
    }*/
}
